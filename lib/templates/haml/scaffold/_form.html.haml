= simple_form_for(@<%= singular_table_name %>) do |f|
  = f.error_notification

  .inputs
  <%- attributes.each do |attribute| -%>
    %p= f.<%= attribute.reference? ? :association : :input %> :<%= attribute.name %>
  <%- end -%>

  .actions
    = f.button :submit
