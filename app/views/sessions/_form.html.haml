= simple_form_for @user, :url => sessions_path, :html => {:id => 'new_session'} do |f|
  = f.error_notification
  .inputs
    = f.input :email, :input_html => {:class => :large}
    = f.input :password
  .actions
    = f.button :submit, "Entrar"